#include <stdio.h>
#include <vector>
#include <string>
using namespace std;
class Solution{
public:
vector<vector<string>> maop(vector<string>& str){
    if(str.size()==0)
    return result;
}
};
int main(){
    return 0;
}